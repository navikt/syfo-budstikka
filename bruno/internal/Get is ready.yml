info:
  name: Get is ready
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/internal/health/is_ready"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
