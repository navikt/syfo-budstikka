# Triage labels

Existing GitHub labels are authoritative. Query the repository before applying
a label, use its exact name, and apply it only with explicit human
authorization.

Do not create, guess, approximate, or silently omit a label required by a
workflow. If a workflow requires a label that is not present in the
repository, stop before the tracker write and ask for a decision.
