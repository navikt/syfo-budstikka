ktor {
  deployment {
    port = 8080
  }
  application {
    modules = [ no.nav.budstikka.ApplicationKt.module ]
  }
}

database {
  host = ${?BUDSTIKKA_DB_HOST}
  port = ${?BUDSTIKKA_DB_PORT}
  name = ${?BUDSTIKKA_DB_DATABASE}
  username = ${?BUDSTIKKA_DB_USERNAME}
  password = ${?BUDSTIKKA_DB_PASSWORD}
  url = "postgresql://"${database.host}":"${database.port}"/"${database.name}
  url = ${?BUDSTIKKA_DB_URL}
  sslkey = ${?BUDSTIKKA_DB_SSLKEY_PK8}

  hikari {
    maximumPoolSize = 3
    minimumIdle = 1
    connectionTimeout = 10000
    idleTimeout = 300000
    maxLifetime = 1800000
  }
}

kafka {
  bootstrapServers = "localhost:9092"
  bootstrapServers = ${?KAFKA_BOOTSTRAP_SERVERS}
  bootstrapServers = ${?KAFKA_BROKERS}

  consumers {
    formidling {
      enabled = ${?KAFKA_FORMIDLING_ENABLED}
      topic = "team-esyfo.formidling.v1"
      topic = ${?KAFKA_FORMIDLING_TOPIC}
      groupId = "syfo-budstikka-formidling-v1"
      groupId = ${?KAFKA_FORMIDLING_GROUP_ID}
      autoOffsetReset = "earliest"
      autoOffsetReset = ${?KAFKA_FORMIDLING_AUTO_OFFSET_RESET}
      maxPollRecords = 100
      maxPollRecords = ${?KAFKA_FORMIDLING_MAX_POLL_RECORDS}
    }
  }

  producers {
    minside {
      topic = "min-side.aapen-microfrontend-v1"
      topic = ${?KAFKA_MINSIDE_TOPIC}
    }
  }

  truststorePath = ${?KAFKA_TRUSTSTORE_PATH}
  keystorePath = ${?KAFKA_KEYSTORE_PATH}
  credentialStorePassword = ${?KAFKA_CREDSTORE_PASSWORD}
}

tasks {
  inboxMessage {
    intervalSeconds = 5
    intervalSeconds = ${?TASK_INBOX_MESSAGE_INTERVAL_SECONDS}
    batchSize = 100
    batchSize = ${?TASK_INBOX_MESSAGE_BATCH_SIZE}
  }
}
