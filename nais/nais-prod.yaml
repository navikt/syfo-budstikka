apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: syfo-budstikka
  namespace: team-esyfo
  labels:
    team: team-esyfo
  annotations:
    texas.nais.io/enabled: "true"
spec:
  image: {{image}}
  port: 8080
  liveness:
    path: /internal/health/is_alive
    initialDelay: 10
    timeout: 5
    periodSeconds: 10
    failureThreshold: 5
  readiness:
    path: /internal/health/is_ready
    initialDelay: 10
    periodSeconds: 5
    timeout: 5
  # Resources: memory request = limit for predictable scheduling and to avoid OOM under node pressure
  # (JVM MaxRAMPercentage=75 → ~768Mi heap at 1024Mi). No CPU limit (avoids throttling); the CPU
  # request is slightly above dev for steadier scheduling under consumer load.
  resources:
    limits:
      memory: 1024Mi
    requests:
      cpu: 200m
      memory: 512Mi
  # Pod count: minimum 2 provides HA, so the application survives rolling deployments and node loss.
  # Maximum 4 leaves capacity to drain delivery/inbox backlog (workers are not partition-bound: they
  # poll the database with SKIP LOCKED, B27, so N equal instances are safe). No leaderElection:
  # periodic sweeps coordinate through a Postgres advisory lock (B12/B42).
  replicas:
    min: 2
    max: 4
  prometheus:
    enabled: true
    path: /internal/metrics # POD_METRICS_PATH; aligns with B57/B48 (metrics, not prometheus)
  observability:
    logging:
      destinations:
        - id: loki
    autoInstrumentation:
      enabled: true
      runtime: java # OTel-agent setter W3C traceparent → Tempo (B45/B48)
  kafka:
    pool: nav-prod # topic: nais/topics/kafka-prod.yaml (team-esyfo.budstikka.v1)
  azure:
    application:
      enabled: true
  accessPolicy:
    outbound:
      rules:
        - application: digdir-krr-proxy
          namespace: team-rocket
      external:
        - host: pdl-api.prod-fss-pub.nais.io
        - host: dokdistfordeling.prod-fss-pub.nais.io
  gcp:
    sqlInstances:
      - name: budstikka-instance
        collation: nb_NO
        type: POSTGRES_18
        tier: db-custom-1-5120
        diskAutoresize: true
        diskAutoresizeLimit: 20
        # No pointInTimeRecovery: the complete database derives from Kafka (inbox/delivery/dead_letter)
        # and can be rebuilt with 90-day replay (B26) and idempotent consumption. Daily automatic
        # backups cover total database loss. Enable it if the RPO requirement tightens.
        # No regional highAvailability: it doubles cost, while a short database failover is not critical
        # here: Kafka retains messages for 90 days (B26) and consumption is idempotent (retry/replay).
        # Enable it if RPO/RTO requirements tighten.
        databases:
          - name: budstikka-db
            # application.conf reads BUDSTIKKA_DB_URL/_USERNAME/_PASSWORD/_SSLKEY_PK8
            envVarPrefix: BUDSTIKKA_DB
  env:
    - name: PDL_URL
      value: https://pdl-api.prod-fss-pub.nais.io/graphql
    - name: PDL_SCOPE
      value: api://prod-fss.pdl.pdl-api/.default
    - name: DOCUMENT_DISTRIBUTOR_URL
      value: https://dokdistfordeling.prod-fss-pub.nais.io/rest/v1/distribuerjournalpost
    - name: DOCUMENT_DISTRIBUTOR_SCOPE
      value: api://prod-fss.teamdokumenthandtering.dokdistfordeling/.default
    - name: KRR_URL
      value: https://digdir-krr-proxy.intern.nav.no/rest/v1/person
    - name: KRR_SCOPE
      value: api://prod-gcp.team-rocket.digdir-krr-proxy/.default
