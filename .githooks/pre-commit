#!/usr/bin/env bash
# Pre-commit gate. Enable once per clone:
#   git config core.hooksPath .githooks
# Enforces that PII/secrets never live in staged added, copied, or modified files.
set -euo pipefail
root="$(git rev-parse --show-toplevel)"
exec bash "$root/scripts/scan-sensitive-data.sh"
